package com.stablecoinbackend.demo.enums;

public class Enums {
    public enum ApprovalStatus {
        APPROVED, PENDING, REJECTED
    }

    public enum TransferType {
        INCOMING, OUTGOING
    }
}
